# Определение базового образа
FROM node:20.9.0

# Установка рабочей директории в контейнере
WORKDIR /usr/src/app

# Копирование файлов `package.json` и `package-lock.json` (если есть)
COPY package*.json ./

# Установка зависимостей проекта
RUN npm install

# Копирование всех файлов проекта в рабочую директорию
COPY . .

# Открытие порта, указанного в переменной окружения PORT
EXPOSE $PORT

# Команда для запуска приложения
CMD ["npm", "start"]
